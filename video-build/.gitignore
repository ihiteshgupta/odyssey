# Build intermediates — regenerated by build.py on every run.
# Tracked: build.py (generator), assets/ (inputs), odyssey-demo.mp4 (deliverable).
slides/
audio/
clips/
*.html
concat.txt
_hs_path.txt
_t.png
capture/
